Boxes_b (denser corridors) — single-line prompts for CLI paste

Copy ONE line below into the CLI (no line breaks inside the prompt).

=== EASY — copy the next line only ===
You are on the boxes_b map (world: boxes_b). Send SmallDeliveryRobot_0 to the NW cache, then to the collection area. Use tools to look up landmark coordinates and navigate. Report when done.

=== MEDIUM — copy the next line only ===
You are on the boxes_b map (world: boxes_b). There are three caches (NW, NE, SE) and one collection area. Allocate SmallDeliveryRobot_0..SmallDeliveryRobot_3 so each cache is visited and robots finish at the collection area. Look up landmarks via tools. Corridors are tight — avoid head-on conflicts. Report when complete.

=== HARD — copy the next line only ===
You are on the boxes_b map (world: boxes_b). With six robots, cover NW/NE/SE caches and gather at the collection area as quickly as possible. Allocate, navigate, resolve corridor conflicts. Report when complete.
